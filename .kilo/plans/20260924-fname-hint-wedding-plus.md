# Поле записи: «Название события», «+» = свадьба, placeholder «Событие»

## Контекст

- `LunarReturns/index.html:562` — input `#fName`:
  - `placeholder="Имя"`
  - `aria-label="Имя события&#10;Короткое имя человека или события — попадёт в список и в результат расчёта."`
- Поле теперь описывает не только имя человека, но и пару («Иван+Мария»), поэтому термин «имя» заменяется на «название».
- `index.html:1386` — статус при пустом поле/дате: `Укажите имя и дату события`.
- Текст aria-label («Заголовок\nТекст») используется и режимом подсказок (❓): панель `#helpPanel` читает его в `index.html:3746`, заголовок панели — первая строка aria-label.
- Логика «+»: `ageSuffix()` (`index.html:1616-1620`) — если в названии есть «+», к годовщине добавляется название свадебной годовщины из `WED` («ситцевая», «серебряная», «золотая»…). То же в push-уведомлениях (`sw.js:128`).

## Правки

1. `LunarReturns/index.html:562`:
   - `placeholder="Имя"` → `placeholder="Событие"`
   - aria-label → `Название события&#10;Короткое название человека или события — попадёт в список. Если в названии есть «+», это свадьба: к годовщине добавится её название (серебряная, золотая…).`
     - заголовок подсказки — «Название события»;
     - упоминание «и в результат расчёта» убрано;
     - добавлено правило «+ = свадьба».
2. `LunarReturns/index.html:1386`: `Укажите имя и дату события` → `Укажите название и дату события`.
3. `LunarReturns/sw.js:1`: `CACHE = "lunarreturns-v13"` → `"lunarreturns-v14"` — конвенция репо: контентные правки `index.html` сопровождаются бампом кэша СВ (пример: коммит 942576d).

## Вне объёма

- Комментарии в коде (`index.html:805` про имя события vk-bridge, `index.html:2930` про maxlength) — не пользовательские, не меняются.
- Логика «+» (`index.html:1619`, `sw.js:128`) и словарь `WED` — без изменений.
- id `fName`, сообщения валидации в консоли/данных — не переименовываются (правится только видимый текст).
- Сборку не запускать (`npm run build` запрещён AGENTS.md — собирает CI при деплое VK).

## Проверка

- `grep "Название события" LunarReturns/index.html`, `grep "Укажите название" LunarReturns/index.html`, `grep "placeholder=\"Событие\"" LunarReturns/index.html`, `grep "lunarreturns-v14" LunarReturns/sw.js`.
- Открыть страницу локально: плейсхолдер поля — «Событие»; пустая отправка формы показывает «Укажите название и дату события»; в режиме подсказок (❓) тап по полю показывает новый текст.
- Ручная проверка логики: запись «Иван+Мария» с датой — в результате у годовщины появляется название (например, «25 серебряная»).
