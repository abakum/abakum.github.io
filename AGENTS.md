# AGENTS.md

## LunarReturns

Не запускать `npm run build` (LunarReturns): сборка в `build/` выполняется
автоматически в `.github/workflows/deploy-lunarreturns-vk.yml` при деплое
на хостинг VK. Локальный запуск не нужен и его следует избегать.
